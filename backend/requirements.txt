fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
# python-multipart 0.0.20 is vulnerable to CVE-2026-24486 (arbitrary file write
# via path traversal) plus several DoS issues fixed through 0.0.32.
python-multipart==0.0.32
# pyjwt 2.10.1 is vulnerable to CVE-2026-48522 (PyJWKClient SSRF/file:// access),
# CVE-2026-48523 (algorithm allow-list bypass with PyJWK keys), CVE-2026-48525
# (unauthenticated DoS via detached JWS) and CVE-2026-48526 (algorithm confusion).
# All fixed in 2.13.0.
pyjwt==2.13.0
bcrypt==4.2.1
# Bumped from 44.0.0: webauthn requires cryptography>=45 (Fernet API unchanged).
# 49.0.0 is vulnerable to GHSA-g6cj-pr64-35w5 / CVE-2026-69247 (PKCS7 decrypt
# functions leak RSA-recovered plaintext length via a timing/response oracle);
# fixed in 50.0.0.
cryptography==50.0.0
pillow==11.0.0
webauthn==2.5.2
